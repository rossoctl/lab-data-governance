// Fixture stand-in for a Vite content-hashed bundle. The SPA-serving tests
// assert this is reachable at /ui/assets/index-fixture.js with a JS media
// type; its contents are irrelevant.
console.log("data-governance ui fixture bundle");
