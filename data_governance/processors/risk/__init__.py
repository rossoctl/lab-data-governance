"""Decision Aggregation & Storage (DAS) — risk processors.

Each risk processor (interaction-risk leg trigger, classification trigger,
trace-risk trigger — issues #99-#102) is its own subpackage here, following
the existing ``processors/<name>/`` sibling-package convention
(``processors/interactions``, ``processors/classification``). No subpackages
exist yet — issue #98 is scaffolding only.
"""
