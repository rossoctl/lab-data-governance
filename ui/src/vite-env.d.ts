/// <reference types="vite/client" />

// Injected by Vite's `define` at build time (see vite.config.ts): short commit
// SHA + date, or 'dev' when git is unavailable.
declare const __APP_VERSION__: string;
