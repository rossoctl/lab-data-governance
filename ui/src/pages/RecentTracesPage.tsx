import { PageSection, Title } from '@patternfly/react-core';

// Placeholder — the recent-traces table lands in the components/pages slice.
export function RecentTracesPage() {
  return (
    <PageSection>
      <Title headingLevel="h2" size="xl">
        Recent traces
      </Title>
    </PageSection>
  );
}
